# v30 IdealThread / CutSpec.full Extrema Route Contract Report

## What was checked against code

- `CNNA/PillarA/Foundation/SubstrateClass.lean` defines `InfiniteThread` by a level-indexed cell and parent-coherence condition.
- `CNNA/PillarA/ToC/Contract.lean` defines `IdealThread` as `InfiniteThread`, sets `IdealThread.carrier L = {U.cell L}`, constructs `IdealThread.family`, and proves deterministic coarsening of the next singleton carrier to the previous one.
- `CNNA/PillarA/Finite/CutSpec.lean` defines `CutSpec.full T` by `carrier := T.carrier`.
- `CNNA/PillarA/Arithmetic/BoundarySource/ExistingIdealThreadNoGo.lean` already records that the existing IdealThread route stays singleton and cannot supply a generated non-singleton boundary carrier by itself.

## Planning changes

- Added green gate `1.1.1.1.0.7`: IdealThread/CutSpec.full extremal-route contract and string-analogy boundary lock.
- Added future phases `1.2.7`, `1.2.8`, `3.0`, `4.0`, and `19.5`.
- Added objects/proof dossiers: `LEG25`, `S07`, `S08`, `BR06`, `BR07`, `LEG26`.
- Added global no-go `GNG13`: no string-like or physical analogy as CNNA-generative input.
- Added workflow policy: Boundary-route extremal audit.
- Added format decisions for extremal routes, string-like interpretation boundary, and thread-recursion provenance.

## Accepted vs. quarantined claims

Accepted as code-backed: IdealThread singleton carrier, DirectedFamily embedding, deterministic coarsening fixed-point, and CutSpec.full maximal carrier.

Accepted as planning obligation: full-boundary matrix geometry must not be sourced from existing IdealThread singleton data unless a new lifting theorem is proved.

Quarantined as comparison-only: string-like physical interpretation and claims that arithmetic invariants are generated by a string-like object. Those may become diagnostics only after the formal route/proof dossiers close.

## Validation

- YAML generation succeeded: 165 phases, 174 objects, 165 scratch-pad records, 174 proof-dossier records.
- PDF compilation is performed separately by `pdflatex`; no Lean build was run or claimed.
